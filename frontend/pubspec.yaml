name: shogyo_mujo_log
description: 諸行無常ログ - 暴食を記録し、修行で劣化を回復するアプリ

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Supabase（バックエンド）
  supabase_flutter: ^2.0.0

  # 画像選択・カメラ
  image_picker: ^1.0.4

  # 歩数計
  pedometer: ^4.0.1

  # HTTP通信（OpenAI API用）
  http: ^1.1.2

  # マテリアルアイコン
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
